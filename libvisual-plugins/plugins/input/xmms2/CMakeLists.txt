INCLUDE_DIRECTORIES(
  ${PROJECT_SOURCE_DIR}
  ${PROJECT_BINARY_DIR}
  ${LIBVISUAL_INCLUDE_DIRS}
  ${XMMS2_INCLUDE_DIRS}
)

LINK_DIRECTORIES(
  ${LIBVISUAL_LIBRARY_DIRS}
  ${XMMS2_LIBRARY_DIRS}
)

SET(input_xmms2_SOURCES
  input_xmms2.c
)

ADD_LIBRARY(input_xmms2 MODULE ${input_xmms2_SOURCES})
#-avoid-version

TARGET_LINK_LIBRARIES(input_xmms2
  ${LIBVISUAL_LIBRARIES}
  ${XMMS2_LIBRARIES}
)

INSTALL(TARGETS input_xmms2 LIBRARY DESTINATION ${LV_INPUT_PLUGIN_DIR})
