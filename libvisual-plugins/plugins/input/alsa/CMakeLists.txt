INCLUDE_DIRECTORIES(
  ${PROJECT_SOURCE_DIR}
  ${PROJECT_BINARY_DIR}
  ${LIBVISUAL_INCLUDE_DIRS}
  ${ALSA_INCLUDE_DIR}
)

LINK_DIRECTORIES(
  ${LIBVISUAL_LIBRARY_DIRS}
)

SET(input_alsa_SOURCES
  input_alsa.c
)

ADD_LIBRARY(input_alsa MODULE ${input_alsa_SOURCES})

SET_TARGET_PROPERTIES(input_alsa PROPERTIES
  LINK_FLAGS -Wl,--unresolved-symbols,ignore-in-shared-libs
)

TARGET_LINK_LIBRARIES(input_alsa
  ${LIBVISUAL_LIBRARIES}
  ${ALSA_LIBRARY}
)

INSTALL(TARGETS input_alsa LIBRARY DESTINATION ${LV_INPUT_PLUGIN_DIR})
