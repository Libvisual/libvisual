INCLUDE_DIRECTORIES(
  ${PROJECT_SOURCE_DIR}
  ${PROJECT_BINARY_DIR}
  ${LIBVISUAL_INCLUDE_DIRS}
  ${JACK_INCLUDE_DIRS}
)

LINK_DIRECTORIES(
  ${LIBVISUAL_LIBRARY_DIRS}
  ${JACK_LIBRARY_DIRS}
)

SET(input_jack_SOURCES
  input_jack.c
)

ADD_LIBRARY(input_jack MODULE ${input_jack_SOURCES})
#-avoid-version

TARGET_LINK_LIBRARIES(input_jack
  ${LIBVISUAL_LIBRARIES}
  ${JACK_LIBRARIES}
)

INSTALL(TARGETS input_jack LIBRARY DESTINATION ${LV_INPUT_PLUGIN_DIR})
