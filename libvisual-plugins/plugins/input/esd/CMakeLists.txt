INCLUDE_DIRECTORIES(
  ${PROJECT_SOURCE_DIR}
  ${PROJECT_BINARY_DIR}
  ${LIBVISUAL_INCLUDE_DIRS}
  ${ESD_INCLUDE_DIRS}
)

LINK_DIRECTORIES(
  ${LIBVISUAL_LIBRARY_DIRS}
  ${ESD_LIBRARY_DIRS}
)

SET(input_esd_SOURCES
  input_esd.c
)

ADD_LIBRARY(input_esd MODULE ${input_esd_SOURCES})
#-avoid-version

TARGET_LINK_LIBRARIES(input_esd
  ${LIBVISUAL_LIBRARIES}
  ${ESD_LIBRARIES}
)

INSTALL(TARGETS input_esd LIBRARY DESTINATION ${LV_INPUT_PLUGIN_DIR})
