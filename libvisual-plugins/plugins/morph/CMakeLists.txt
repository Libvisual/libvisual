IF(ENABLE_ALPHABLEND)
  ADD_SUBDIRECTORY(alphablend)
ENDIF(ENABLE_ALPHABLEND)

IF(ENABLE_FLASH)
  ADD_SUBDIRECTORY(flash)
ENDIF(ENABLE_FLASH)

IF(ENABLE_TENTACLE)
  ADD_SUBDIRECTORY(tentacle)
ENDIF(ENABLE_TENTACLE)

IF(ENABLE_SLIDE)
  ADD_SUBDIRECTORY(slide)
ENDIF(ENABLE_SLIDE)
