IF(ENABLE_BLURSK)
  ADD_SUBDIRECTORY(blursk)
ENDIF()

IF(ENABLE_BUMPSCOPE)
  ADD_SUBDIRECTORY(bumpscope)
ENDIF()

IF(ENABLE_CORONA)
  ADD_SUBDIRECTORY(corona)
ENDIF()

IF(ENABLE_DANCINGPARTICLES)
  ADD_SUBDIRECTORY(dancingparticles)
ENDIF()

IF(ENABLE_FLOWER)
  ADD_SUBDIRECTORY(flower)
ENDIF()

IF(ENABLE_GDKPIXBUF)
  ADD_SUBDIRECTORY(gdkpixbuf)
ENDIF()

IF(ENABLE_GFORCE)
  ADD_SUBDIRECTORY(gforce)
ENDIF()

IF(ENABLE_GOOM2K4)
  ADD_SUBDIRECTORY(goom2k4)
ENDIF()

IF(ENABLE_GSTREAMER)
  ADD_SUBDIRECTORY(gstreamer)
ENDIF()

IF(ENABLE_INFINITE)
  ADD_SUBDIRECTORY(infinite)
ENDIF()

IF(ENABLE_JAKDAW)
  ADD_SUBDIRECTORY(jakdaw)
ENDIF()

IF(ENABLE_JESS)
  ADD_SUBDIRECTORY(jess)
ENDIF()

IF(ENABLE_LCDCONTROL)
  ADD_SUBDIRECTORY(lcdcontrol)
ENDIF()

IF(ENABLE_LV_ANALYZER)
  ADD_SUBDIRECTORY(lv_analyzer)
ENDIF()

IF(ENABLE_LV_DUMP)
  ADD_SUBDIRECTORY(lv_dump)
ENDIF()

IF(ENABLE_LV_GLTEST)
  ADD_SUBDIRECTORY(lv_gltest)
ENDIF()

IF(ENABLE_LV_SCOPE)
  ADD_SUBDIRECTORY(lv_scope)
ENDIF()

IF(ENABLE_MADSPIN)
  ADD_SUBDIRECTORY(madspin)
ENDIF()

IF(ENABLE_NASTYFFT)
  ADD_SUBDIRECTORY(nastyfft)
ENDIF()

IF(ENABLE_NEBULUS)
  ADD_SUBDIRECTORY(nebulus)
ENDIF()

IF(ENABLE_OINKSIE)
  ADD_SUBDIRECTORY(oinksie)
ENDIF()

IF(ENABLE_PLAZMA)
  ADD_SUBDIRECTORY(plazma)
ENDIF()
