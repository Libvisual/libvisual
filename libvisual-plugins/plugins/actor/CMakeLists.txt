IF(ENABLE_BLURSK)
  ADD_SUBDIRECTORY(blursk)
ENDIF(ENABLE_BLURSK)

IF(ENABLE_BUMPSCOPE)
  ADD_SUBDIRECTORY(bumpscope)
ENDIF(ENABLE_BUMPSCOPE)

IF(ENABLE_CORONA)
#  ADD_SUBDIRECTORY(corona)
ENDIF(ENABLE_CORONA)

IF(ENABLE_GDKPIXBUF)
  ADD_SUBDIRECTORY(gdkpixbuf)
ENDIF(ENABLE_GDKPIXBUF)

IF(ENABLE_GFORCE)
  ADD_SUBDIRECTORY(G-Force)
ENDIF(ENABLE_GFORCE)

IF(ENABLE_GSTREAMER)
  ADD_SUBDIRECTORY(gstreamer)
ENDIF(ENABLE_GSTREAMER)

  # infinite
  # jakdaw
  # JESS
  # lv_analyzer
  # lv_gltest
  # lv_scope
  # madspin
  # nastyfft
  # oinksie
  # pseudotoad_flower
  # blursk

