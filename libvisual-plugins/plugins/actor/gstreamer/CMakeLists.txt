INCLUDE_DIRECTORIES(
  ${PROJECT_SOURCE_DIR}
  ${PROJECT_BINARY_DIR}
  ${LIBVISUAL_INCLUDE_DIRS}
  ${GSTREAMER_INCLUDE_DIRS}
)

LINK_DIRECTORIES(
  ${LIBVISUAL_LIBRARY_DIRS}
  ${GSTREAMER_LIBRARY_DIRS}
)

SET(actor_gstreamer_SOURCES
  actor_gstreamer.c
)

ADD_LIBRARY(actor_gstreamer MODULE ${actor_gstreamer_SOURCES})
#-avoid-version

TARGET_LINK_LIBRARIES(actor_gstreamer
  ${LIBVISUAL_LIBRARIES}
  ${GSTREAMER_LIBRARIES}
)

INSTALL(TARGETS actor_gstreamer LIBRARY DESTINATION ${LV_ACTOR_PLUGIN_DIR})
