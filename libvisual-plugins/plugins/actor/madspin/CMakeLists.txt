SET(MADSPIN_DATA_DIR ${LV_PLUGIN_DATA_DIR}/actor/actor_madspin)

LV_BUILD_ACTOR_PLUGIN(madspin
  SOURCES      madspin.c
  COMPILE_DEFS -DSTAR_DIR="${MADSPIN_DATA_DIR}"
  INCLUDE_DIRS ${OPENGL_INCLUDE_DIR}
  LINK_LIBS    ${OPENGL_gl_LIBRARY} ${OPENGL_glu_LIBRARY} m
)

SET(IMAGES
  star1.bmp
  star2.bmp
)

INSTALL(FILES ${IMAGES} DESTINATION ${MADSPIN_DATA_DIR})
