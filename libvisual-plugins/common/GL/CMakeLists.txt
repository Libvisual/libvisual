INCLUDE_DIRECTORIES(
  ${PROJECT_SOURCE_DIR}
  ${PROJECT_BINARY_DIR}
)

ADD_LIBRARY(miniGLU STATIC glu.c)

SET_TARGET_PROPERTIES(miniGLU
  PROPERTIES COMPILE_FLAGS -fPIC
)

TARGET_LINK_LIBRARIES(miniGLU
  OpenGL::GL
)
