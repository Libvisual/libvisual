INCLUDE_DIRECTORIES(
  ${PROJECT_SOURCE_DIR}
  ${PROJECT_BINARY_DIR}
  ${OPENGL_INCLUDE_DIR}
)

ADD_LIBRARY(GLU STATIC glu.c)

SET_TARGET_PROPERTIES(GLU
  PROPERTIES COMPILE_FLAGS -fPIC
)

TARGET_LINK_LIBRARIES(GLU
  ${OPENGL_gl_LIBRARY}
)
